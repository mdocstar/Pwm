[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pwm"
version = "0.1.0"
description = "Three-phase PWM voltage analysis and visualization"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
